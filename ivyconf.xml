<ivysettings>
	<conf defaultResolver="filesystem" />
	<resolvers>
		<filesystem name="filesystem">
			<ivy pattern="${basedir}/tools/thirdparty/[organisation]/[module]/[revision]/ivy-[revision].xml"/>
			<artifact pattern="${basedir}/tools/thirdparty/[organisation]/[module]/[revision]/[type]s/[artifact]-[revision].[ext]"/>
		</filesystem>
	</resolvers>
</ivysettings>
